{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "OPENAI_API_KEY": "@openai_api_key",
    "NEXT_PUBLIC_APP_URL": "https://policylabs.vercel.app"
  },
  "build": {
    "env": {
      "OPENAI_API_KEY": "@openai_api_key"
    }
  }
}